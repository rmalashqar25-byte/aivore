# Contributing

Thank you for helping improve Aivora.

Before opening a pull request, run:

```sh
dart format lib test
flutter analyze
flutter test
```

Keep changes focused, maintain both English and Arabic content, verify right-to-left layout, and add tests for changed behavior. Never commit API keys, private user data, signing keys, generated builds, or release APKs.
